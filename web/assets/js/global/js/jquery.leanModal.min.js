// leanModal v1.1 by Ray Stone - http://finelysliced.com.au
// Dual licensed under the MIT and GPL
define(['jquery'], function($){
	(function($){$.fn.extend({leanModal:function(options){var defaults={top:100,overlay:0.5,closeButton:null};var overlay=$("<div id='lean_overlay'></div>");$("body").append(overlay);options=$.extend(defaults,options);return this.each(function(){var o=options;$(this).click(function(e){var modal_id=$(this).attr("href");$("#lean_overlay").click(function(){close_modal(modal_id)});$(o.closeButton).click(function(e){e.preventDefault();close_modal(modal_id)});var modal_height=$(modal_id).outerHeight();var modal_width=$(modal_id).outerWidth();
	$("#lean_overlay").css({"display":"block",opacity:0});$("#lean_overlay").fadeTo(300,o.overlay);$(modal_id).css({"display":"block","position":"fixed","opacity":0,"z-index":11000,"left":50+"%","margin-left":-(modal_width/2)+"px","top":o.top+"px"}).addClass('open');$(modal_id).fadeTo(300,1);
	var isTouchDevice = ('ontouchstart' in window) || window.DocumentTouch && document instanceof DocumentTouch,
		isWinPhoneDevice = /Windows Phone/.test(navigator.userAgent),
		pageWidth = jQuery(window).width();

		if((isTouchDevice || isWinPhoneDevice) && pageWidth < 1200){
			//jcf.destroyAll();
		} else {
			jcf.replaceAll();
		}
	e.preventDefault()})});function close_modal(modal_id){$("#lean_overlay").fadeOut(400);$(modal_id).fadeOut(400).removeClass('open');}}})})(jQuery);
});